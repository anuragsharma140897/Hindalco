import { Title, Text, ActionIcon, Button, Popover } from 'rizzui';
import { PiTrashFill } from 'react-icons/pi';
import TrashIcon from '../../Constant/Icons/trash';

export default function SearchUser({ title, description, onDelete }) {
    return (
        <div>
            <h1>Search</h1>
        </div>
    );
}
